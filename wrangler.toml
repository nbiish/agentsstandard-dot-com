# Cloudflare Pages configuration
# Static site — no build step needed
name = "agentsstandard-dot-com"
compatibility_date = "2026-05-26"

# Output directory is the root (pure static HTML)
[site]
bucket = "."
